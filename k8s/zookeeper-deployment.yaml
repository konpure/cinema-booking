apiVersion: v1
kind: Service
metadata:
  name: zookeeper-svc
  namespace: cinema
spec:
  ports:
  - port: 2181
    targetPort: 2181
  selector:
    app: zookeeper
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: zookeeper
  namespace: cinema
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zookeeper
  template:
    metadata:
      labels:
        app: zookeeper
    spec:
      containers:
      - name: zookeeper
        image: zookeeper:3.9
        ports:
        - containerPort: 2181
        livenessProbe:
          exec:
            command: ["zkServer.sh", "status"]
          initialDelaySeconds: 15
          periodSeconds: 10