# 星影 Cinema 影院订票系统 — 组员入门（请先读）

> 软件架构设计实践 · 课程大作业  
> 包内为完整项目源码，解压后按本文操作即可运行。  
> **详细步骤见同目录：`本地运行指南-组员版.md`**

**包版本：2026-06-08**（含 5 影城 / 10 影片 / 多场次 / 前端体验优化）

---

## 一、5 分钟快速启动

### 环境要求

| 软件 | 版本建议 | 说明 |
|------|---------|------|
| JDK | 17 | 本地跑后端必装 |
| MySQL | 8.x | 本地模式必装 |
| Redis | 6379 | 本地模式必装 |
| Erlang | **27.x** | RabbitMQ 依赖，不要用 29 |
| RabbitMQ | 4.x | 本地模式必装 |
| Node.js | 18+ | 本地前端必装 |
| Docker Desktop | 可选 | **有 Docker 的同学推荐用方式 B** |

### 方式 A：本地模式（PowerShell）

```powershell
# 进入解压后的项目根目录（本文件夹）

# ① 启动 RabbitMQ
cd docker
.\start-rabbitmq.ps1

# ② 配置 MySQL 密码（只需做一次）
cd ..\backend
copy src\main\resources\application-local.yml.example src\main\resources\application-local.yml
# 用记事本打开 application-local.yml，把 password 改成你的 MySQL root 密码

# ③ 启动后端（看到 Started CinemaApplication 即成功）
.\run-backend.ps1

# ④ 启动前端（新开一个 PowerShell 窗口）
cd ..\frontend
npm install
npm run dev
```

浏览器打开：**http://localhost:5173**

### 方式 B：Docker Desktop（一键部署）

```powershell
cd docker
docker compose up -d --build
```

浏览器打开：**http://localhost**（注意不是 5173）

| 账号 | 密码 | 角色 |
|------|------|------|
| demo | demo123 | 普通用户 |
| admin | admin123 | 管理员 |

---

## 二、项目里有什么

```
本文件夹/
├── 【请先读我】组员入门.md     ← 你正在读
├── 本地运行指南-组员版.md       详细启动步骤
├── 课程架构技术-用到与未用到.md  答辩必看：技术清单对照
├── README.md                   项目说明
├── backend/                    Spring Boot 后端
├── frontend/                   Vue3 前端
├── docker/                     Docker Compose、RabbitMQ 脚本
└── docs/                       技术报告、分工、架构对照
```

**必读文档：**

| 文件 | 用途 |
|------|------|
| **`课程架构技术-用到与未用到.md`** | **课程 PPT 全清单：哪些用了、哪些没用、用在哪** |
| `docs/小组协作与项目状态.md` | 大作业要求、分工任务、答辩流程 |
| `docs/架构技术-文件对照表.md` | 每项架构技术对应哪个源码文件 |
| `docs/技术总结报告.md` | 提交用报告草稿（需小组合并润色） |
| `docs/Docker-Toolbox-部署指南.md` | Docker 完整栈说明 |

---

## 三、当前系统内容（答辩演示）

| 内容 | 数量 |
|------|------|
| 影城 | 5 家（朝阳 / 海淀 / 西单 / 上海浦东 / 广州天河） |
| 热映影片 | 10 部 |
| 场次 | 每家影城 × 每部影片至少 4 场（今天 + 明天） |
| 卖品 | 爆米花、可乐、情侣/家庭套餐等 |

**切换顶部影城** → 首页只显示该店有排片的影片 → 点进详情可选多个场次。

首次启动后端会自动建库并插入/补齐演示数据；日志出现 `Enriched catalog` 表示场次已补齐。

---

## 四、课程要求提醒

1. **可用性**：两个场景（抢票高峰 + 实例故障）——见 `docs/技术总结报告.md`
2. **每人 ≥3 种架构技术**（MySQL / HTML / CSS / Git / Maven **不算**）
3. 答辩时技术需**单机集成可演示**（Docker 同学可演示 Nginx 负载均衡）
4. **功能不必做满**，场景合理即可

已实现架构技术：Spring Security、Redis、RabbitMQ、Nginx 负载均衡、Docker Compose、Vue3、ECharts、SLF4J、JUnit

---

## 五、分工任务（请认领）

在 `docs/小组协作与项目状态.md` 第六节填写「每人 3 技术认领表」，并认领下列任务：

| 编号 | 任务 | 优先级 |
|------|------|--------|
| T1 | 技术总结报告定稿 | 高 |
| T2 | 答辩 PPT | 高 |
| T3 | 管理后台 / 前端继续优化 | 中 |
| T4 | Sentinel 限流 demo | 中 |
| T5 | Nginx 双实例演示说明 | 中 |
| T6 | JUnit 测试补充 | 低 |
| T7 | 5 分钟答辩排练 | 高 |
| T8 | 每人 3 技术认领 | 高 |

建议各自建 Git 分支：`feature/姓名-任务名`，改完发给组长合并。

---

## 六、常见问题

**Q：后端启动报 MySQL 连接失败？**  
A：检查 `application-local.yml` 里密码是否正确；MySQL 服务是否已启动（3306 端口）。

**Q：后端启动报 RabbitMQ / Redis 连接失败？**  
A：先运行 `docker/start-rabbitmq.ps1`；确认 Redis 6379 已启动。或用 Docker 方式 B 一键启动。

**Q：切换影城后没有影片或场次？**  
A：重启后端让数据补齐逻辑执行；仍异常则 MySQL 执行 `docs/init-local-db.sql` 后重启。

**Q：返回上一页闪一下白色横条？**  
A：已优化，需用本包最新前端；刷新页面或重新 `npm run dev`。

**Q：RabbitMQ 管理台？**  
A：http://localhost:15672 ，账号 `guest` / `guest`

---

## 七、演示流程（答辩用）

1. 顶部切换不同影城 → 选电影 → 选场次  
2. 登录 demo → 选座 → 加购卖品 → 支付  
3. 「我的订单」查看明细  
4. admin 登录 → 管理后台图表 + 影片/场次/卖品管理  
5. （Docker 同学）多次访问 `/api/health` 看负载均衡  
6. （可选）RabbitMQ 管理台看队列消息  

有问题在小组群 @ 组长。
