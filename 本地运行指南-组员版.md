# 本地运行指南（组员版）

> 拿到小组作业 zip 包并解压后，按本文从零跑通项目。  
> **包版本：2026-06-08**

---

## 0. 解压后先看目录

```
cinema-booking-发给组员/          （或你解压后的文件夹名）
├── 【请先读我】组员入门.md        ← 先看这个
├── 本地运行指南-组员版.md         ← 本文
├── 课程架构技术-用到与未用到.md   ← 答辩技术对照
├── backend/
├── frontend/
├── docker/
└── docs/
```

---

## 1. 选一种启动方式

| 方式 | 适合谁 | 访问地址 |
|------|--------|---------|
| **A 本地模式** | 无 Docker / 习惯分开启动 | http://localhost:5173 |
| **B Docker Desktop** | 已装 Docker，想一键演示完整栈 | http://localhost |

---

## 2. 方式 A：本地模式

### 2.1 安装环境（首次必做）

| 软件 | 作用 | 检查方式 |
|------|------|---------|
| **JDK 17** | 运行后端 | `java -version` 显示 17 |
| **MySQL 8** | 数据库 | 服务已启动，端口 3306 |
| **Redis** | 座位锁 | 端口 6379 可连 |
| **Erlang 27.x** | RabbitMQ 依赖 | 不要用 Erlang 29 |
| **RabbitMQ 4.x** | 消息队列 | 端口 5672 可连 |
| **Node.js 18+** | 运行前端 | `node -v`、`npm -v` |

```powershell
Test-NetConnection localhost -Port 3306   # MySQL
Test-NetConnection localhost -Port 6379   # Redis
Test-NetConnection localhost -Port 5672   # RabbitMQ
```

或：`cd docker` → `.\check-local.ps1`

### 2.2 配置 MySQL 密码（只需一次）

```powershell
cd backend
copy src\main\resources\application-local.yml.example src\main\resources\application-local.yml
```

编辑 `application-local.yml`，填入你的 MySQL 密码：

```yaml
spring:
  datasource:
    username: root
    password: "你的MySQL密码"
```

### 2.3 启动顺序（每次运行，3 个窗口）

**窗口 1 — RabbitMQ**

```powershell
cd docker
.\start-rabbitmq.ps1
```

**窗口 2 — 后端**

```powershell
cd backend
.\run-backend.ps1
```

成功标志：`Started CinemaApplication`  
首次启动会自动插入 **5 影城 / 10 影片 / 多场次** 演示数据。

**窗口 3 — 前端**

```powershell
cd frontend
npm install          # 仅第一次
npm run dev
```

访问：**http://localhost:5173**

---

## 3. 方式 B：Docker Desktop

前提：已安装并启动 **Docker Desktop**（Windows 建议开启 WSL2 后端）。

```powershell
cd docker
docker compose up -d --build
```

首次构建约 5～15 分钟，完成后：

| 服务 | 地址 |
|------|------|
| 网站 | http://localhost |
| RabbitMQ 管理台 | http://localhost:15672（guest/guest） |
| 健康检查 / 负载均衡 | http://localhost/api/health（多刷新看 instance 变化） |

停止：`docker compose down`

详细说明见 `docs/Docker-Toolbox-部署指南.md`

---

## 4. 登录与功能自测

| 账号 | 密码 | 用途 |
|------|------|------|
| demo | demo123 | 选座、卖品、订单 |
| admin | admin123 | 管理后台、图表 |

建议流程：

1. 顶部切换**不同影城**（5 家可选）  
2. 首页选电影（按影城过滤，应有排片）  
3. 选场次 → 登录 demo → 选座 → 加卖品 → 支付  
4. 「我的订单」查看  
5. admin 登录 → 管理后台  

---

## 5. 用 IDEA 运行后端（可选）

1. IDEA 打开 `backend`，JDK 选 **17**  
2. 环境变量：`SPRING_PROFILES_ACTIVE=local`  
3. 运行 `com.cinema.CinemaApplication`  
4. 前端仍用 `npm run dev`  

---

## 6. 常见问题

### MySQL 连接失败

- 检查 `application-local.yml` 密码  
- 确认 MySQL 3306 已启动  

### Redis / RabbitMQ 连接失败

- 本地模式：分别确认 6379、5672 端口  
- 或改用 Docker 方式 B  

### 切换影城没有影片

- 重启后端（会自动补齐场次）  
- 仍异常：MySQL 执行 `docs/init-local-db.sql`，再重启后端  

### 前端能开但接口报错

- 确认后端已启动  
- 本地模式访问 5173，Docker 模式访问 80  

### npm install 很慢

```powershell
npm config set registry https://registry.npmmirror.com
```

---

## 7. 协作与答辩文档

| 文档 | 内容 |
|------|------|
| `课程架构技术-用到与未用到.md` | **课程技术清单对照（必看）** |
| `docs/小组协作与项目状态.md` | 分工认领、答辩流程 |
| `docs/架构技术-文件对照表.md` | 源码位置 |
| `docs/技术总结报告.md` | 报告草稿 |

每人至少认领 **3 种架构技术**（MySQL / HTML / CSS / Git / Maven 不算）。

---

## 8. 启动命令速查

**本地模式：**

```powershell
cd docker; .\start-rabbitmq.ps1
cd ..\backend; .\run-backend.ps1
cd ..\frontend; npm install; npm run dev
```

**Docker 模式：**

```powershell
cd docker; docker compose up -d --build
```

有问题在小组群 @ 组长，附上**报错截图 + 执行到哪一步**。
